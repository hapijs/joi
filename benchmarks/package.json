{
  "name": "benchmarks",
  "scripts": {
    "test": "npm run bench",
    "bench": "node ./bench.js --compare results.json",
    "bench-update": "npm run bench -- --save results.json"
  },
  "dependencies": {
    "benchmark": "^2.1.4",
    "@hapi/bossy": "^4.0.3",
    "chalk": "^2.4.1",
    "cli-table": "^0.3.1",
    "d3-format": "^1.3.2",
    "@hapi/hoek": "^6.1.2"
  }
}
